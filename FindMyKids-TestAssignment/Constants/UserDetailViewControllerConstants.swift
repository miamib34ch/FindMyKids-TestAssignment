import UIKit

enum UserDetailViewControllerConstants {

    // TODO: поменять на локализацию
    enum Strings {
        static let title: String = "Подписчики"
    }

    enum Layout {
        static let contentOffset: CGFloat = 32.0
        static let contentSpacing: CGFloat = 10.0
        static let cellHeight: CGFloat = 234.0
        static let headerHeight: CGFloat = 40.0
    }

    enum Font {
        static let header: UIFont = .systemFont(ofSize: 18, weight: .bold)
    }
}
